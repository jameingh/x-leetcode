# 198. 打家劫舍 (House Robber)
#
# 题目描述：
# 你是一个专业的小偷，计划偷窃沿街的房屋。每间房内都藏有一定的现金，
# 影响你偷窃的唯一制约因素就是相邻的房屋装有相互连通的防盗系统：
# 如果两间相邻的房屋在同一晚上同时被闯入，系统会自动报警。
# 给定一个代表每个房屋存放金额的非负整数数组 nums，
# 计算你在不触动警报装置的情况下，一夜之内能够偷窃到的最高金额。
#
# 示例：
#   输入：nums = [2, 7, 9, 3, 1]
#   输出：12（偷第 0、2、4 间：2 + 9 + 1 = 12）
#
# 思维模型：【偷 or 不偷的博弈】
#   dp[i] = "考虑前 i 间房"能偷到的最大金额（注意是"考虑"，不是"必偷第 i 间"）
#   站在第 i 间房门口，最后一个决策只有两种：
#     - 不偷第 i 间：前面怎么偷还怎么偷 -> dp[i-1]
#     - 偷第 i 间：第 i-1 间必须放弃   -> dp[i-2] + nums[i]
#   收益高者胜出：dp[i] = max(dp[i-1], dp[i-2] + nums[i])
#
# DP 三步走：
#   1. 定义状态：dp[i] = 考虑前 i 间房（下标 0..i）能偷到的最大金额
#   2. 转移方程：dp[i] = max(dp[i-1], dp[i-2] + nums[i])
#   3. 起点：dp[0] = nums[0]，dp[1] = max(nums[0], nums[1])
from typing import List


class Solution:
    def rob(self, nums: List[int]) -> int:
        # 提示 1：先处理只有 1 间房的情况（起点 dp[1] 需要两间房才存在）
        #
        # 提示 2：起点 dp[0] = nums[0]，dp[1] = max(nums[0], nums[1])
        #         （思考：为什么 dp[1] 不是 nums[1]？）
        #
        # 提示 3：从 2 循环到 len(nums) - 1，每间房做"偷 or 不偷"的 max 决策
        #
        # 提示 4：继续用滚动变量。两句钢印自查：
        #         "覆盖之前先保值"、"钱跟着起跳的台阶走"（这题是：金额跟着被偷的房子走）
        pass


if __name__ == "__main__":
    solution = Solution()

    # 测试用例 1：基础情况（偷 1 + 3 = 4）
    print(solution.rob([1, 2, 3, 1]))  # 期望输出: 4

    # 测试用例 2：大脑模拟过的例子（第 3 间要学会"放过"）
    print(solution.rob([2, 7, 9, 3, 1]))  # 期望输出: 12

    # 测试用例 3：只有一间房
    print(solution.rob([5]))  # 期望输出: 5

    # 测试用例 4：两间房只能挑一间
    print(solution.rob([2, 1]))  # 期望输出: 2

    # 测试用例 5：跳过多间也可能更优（偷 2 + 9，放过中间两间）
    print(solution.rob([2, 1, 1, 9]))  # 期望输出: 11
